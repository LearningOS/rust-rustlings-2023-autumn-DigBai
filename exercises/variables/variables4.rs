// variables4.rs
//
// Execute `rustlings hint variables4` or use the `hint` watch subcommand for a
// hint.



fn main() {
    let mut x = 3;//rust语言声明的赋值过是不能变的 除非mut声明  或者再声明let一次
    println!("Number {}", x);
    x = 5; // don't change this line
    println!("Number {}", x);
}
